# Implementation Plans

Generated by the Improve skill on 2026-08-30 at commit `d160a40`.
Execute in the order below unless dependencies say otherwise. Each executor
must read its plan fully, honor its STOP conditions, and leave index updates to
the reviewer coordinating this combined branch.

## Execution order and status

| Plan | Title                                              | Priority | Effort | Depends on | Status |
| ---- | -------------------------------------------------- | -------- | ------ | ---------- | ------ |
| 001  | Freeze numerical engine outputs                    | P1       | S      | —          | DONE   |
| 002  | Make DTCG dictionaries prototype-safe              | P1       | S      | —          | DONE   |
| 003  | Report the rendered foreground metric              | P1       | S      | —          | DONE   |
| 004  | Keep editor route and Palette identity aligned     | P1       | M      | —          | DONE   |
| 005  | Use D65 for target selection and weight assignment | P1       | S      | 001        | DONE   |

Status values: `TODO`, `IN PROGRESS`, `DONE`, `BLOCKED`, or `REJECTED`.

## Dependency notes

- Plan 001 must land before Plan 005. Plan 005 changes numerical output and
  needs independent literals that make the change reviewable.
- Plans 002, 003, and 004 are independent and may execute in parallel after
  their drift checks pass.
- Run the full repository gate after all five plans: `npm run test`,
  `npm run lint`, `npx tsc --noEmit --incremental false`, and `npm run build`.

## Findings considered and rejected

- Treat all current `npm audit` HIGH entries as application vulnerabilities:
  rejected because their affected paths are not executed by first-party code.
- Add manual React memoization: rejected because React Compiler is enabled and
  the measured hotspot is Color.js conversion inside the engine.
- Add cloud sync: rejected because Tonal Foundry explicitly uses local browser
  persistence and the selected work does not change that product decision.
- Rename engine `semantic` to `name` in this branch: rejected because it has a
  large storage/share/CLI blast radius unrelated to the selected five fixes.
